services:
  api:
    build:
      dockerfile: Dockerfile
      context: ./
    environment:
      - GOFLAGS=-buildvcs=false
    env_file: ".env"
    volumes:
      - ./:/app
    ports:
      - 8080:8080
    # depends_on:
